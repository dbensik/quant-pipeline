import sys

import pytest

if __name__ == "__main__":
    # pytest.main() runs pytest and passes command-line arguments to it.
    # We exit with the status code returned by pytest.
    sys.exit(pytest.main())
