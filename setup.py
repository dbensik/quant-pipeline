from setuptools import setup

# This is the minimal setup.py required for a modern project.
# All configuration is now handled by the pyproject.toml file.
# This file enables editable installs (`pip install -e .`) and provides
# backward compatibility with older tools.
if __name__ == "__main__":
    setup()
